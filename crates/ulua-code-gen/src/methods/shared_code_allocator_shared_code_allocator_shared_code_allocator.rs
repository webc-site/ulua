use crate::records::shared_code_allocator::SharedCodeAllocator;

impl SharedCodeAllocator {
  pub fn shared_code_allocator_shared_code_allocator(&mut self) {
    unreachable!("Deleted constructor");
  }
}
