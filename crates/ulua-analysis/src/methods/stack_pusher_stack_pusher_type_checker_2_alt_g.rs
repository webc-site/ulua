//! @interface-stub
use crate::records::stack_pusher_type_checker_2::StackPusher;

impl StackPusher {
  pub fn stack_pusher(&mut self) -> Self {
    panic!("StackPusher copy constructor is deleted in C++")
  }
}
