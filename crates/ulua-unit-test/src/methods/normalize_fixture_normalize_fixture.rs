//! @interface-stub
use crate::records::normalize_fixture::NormalizeFixture;

impl NormalizeFixture {
  pub fn normalize_fixture(&mut self) {
    // C++ NormalizeFixture constructor is empty; initialization is lazy in getFrontend.
  }
}
