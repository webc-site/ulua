//! @interface-stub
// Orphaned `SUBCASE("both solvers")` skeleton node from the
// type_infer/type_instantiations doctest; doctest reruns the case once per
// subcase, which the importer split into duplicate nodes. No translated test
// drives this helper. Unused scaffolding.
pub fn subcase_both_solvers() {
  unreachable!("orphaned doctest SUBCASE node; no call site")
}
