//! @interface-stub
// Orphaned skeleton node for a C++ `operator==` with no captured type or
// signature and no call site (test-side equality is exercised through the
// concrete types' own `operator_eq` methods). Unused scaffolding.
pub fn operator_eq() {
  unreachable!("orphaned operator== skeleton node; no type info and no call site")
}
