运行 ./fork.sh ，开分支到 /tmp/fork 开发，完成后合并到当前目录，然后删除分支
对照./cpp，修复./test.sh 的报错，然后
 ./sh/clippy.sh，运行，修复所有报警
禁止用 allow，按 rust 最佳实践优化代码
实现参考 ./cpp，但优化代码风格，按 rust 的最佳实践改写（而不是 c、cpp 的代码风格），如:
用引用而不是指针，用元组返回值，而不是c风格的把传入参数当返回值
用 Option<&T> 替代空指针
善用 rust 语言特性，泛型、enum、迭代器、编译期计算等等，深入思考
用 thiserror 配合 error.rs 替代c风格的异常
rg "dyn " -t rs ，思考能不能用泛型、https://crates.io/crates/enum_dispatch 等，避免 dyn，优化效率
参考 .agents/skills/rust_review/SKILL.md ，用高性能 rust 库替代手动实现
rg ffi -t rs ，改为为对应的 rust 类型
清理不必要的别名，冗余的代码，拆分重复代码为函数、模块复用
清理死代码，清理不被使用的变量，清理冗余重复
对照./cpp，清理 ai 生成的垃圾测试，添加 cpp 有的测试
集成测试放到 tests 文件夹下，不要混到 src 代码内部
很多c风格的代码，都改写为 rust 最佳实践的风格，一个一个来（避免用c风格），减少 unsafe
思考如何用 rust 的特性，优化整个代码库的风格、实现、架构，让代码更美
思考，哪些c风格的字符串，可以用 rust 风格的 &str 或者 &[u8] 替换，改写?
不断并发开子代理，思考还有什么可以优化，打磨细节，完善代码，不断循环，自主决策（不需要人确认），直到连续十次（失败就重新计数），每个 crate 子代理认为已经达到生产级别，没有 bug，和 cpp 对齐